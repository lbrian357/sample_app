<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
    <%= render @users %> <%# rails automatically looks for a partial called _user.html.erb because render is called on user variable of class User %><%# rails also iterates through a list of User objects when called with a collection of users (ie using @users instead of user), so a do-end block isn't necessary. In this case rails renders each one with the _user.html.erb partial by inferring the name of the partial from the name of the class.%>
</ul>

<%= will_paginate %>
